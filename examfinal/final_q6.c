#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>


int main(int argc, char *argv[]) {

    // final_q6.c Should print the lines of the file in reverse order.

    // The order of characters within lines should not be changed.

    // The order of characters within lines should be unchanged.
    FILE *fp = fopen(argv[1], "r");
    // creating a extra file
    FILE *fp2 = tmpfile();
    
}
